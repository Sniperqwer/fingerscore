# Python environment
Always use the conda environment fingerscore before running any Python commands.
- Run `conda activate fingerscore` before executing Python scripts
- Use `python` instead of `python3`
- Install packages with `pip install`, never use `conda install`
